interface:
  display_name: "Capelry"
  short_description: "Find, validate, and install cataloged agent skills"
  brand_color: "#5B7CFA"
  default_prompt: "Use $capelry to find a skill for my task, inspect it, identify this harness's verified project target, validate the package, install it, and confirm the harness can see it."
